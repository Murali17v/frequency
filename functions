W= input('Please enter a string ')
list1=[]
def most_frequent(string):
    d = dict()
    for key in string:
        if key not in d:
            d[key] = 1
        else:
            d[key] += 1
    return d
list1=most_frequent(W)

print(list1)
